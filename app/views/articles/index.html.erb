<h1>Articles</h1>

Our Blog has <%= Article.public_count %> Articles and counting!

<ul>
  <% @articles.each do |article| %>
    <% unless article.archived? %> <!-- aqui verifica seo status do artigo na tabela é 'archived', se for nao mostra link --!>
      <li>
      <%= link_to article.title, article %> <!-- para cada artigo pega o id dele e coloca como um link na URL /articles/. article_path helper returns "/articles/#{article.id}" when given an article -->
                                       <!-- mostra o titulo do artigo -->
      </li>
    <% end %>
  <% end %>
</ul>

<%= link_to "New Article", new_article_path %>
